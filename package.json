{
  "name": "thayam",
  "private": true,
  "packageManager": "pnpm@11.24.0",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "test:coverage": "turbo run test:coverage",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "dist:win": "pnpm --filter @thayam/web run build && pnpm --filter @thayam/desktop run dist:win"
  },
  "devDependencies": {
    "turbo": "^2.10.0",
    "typescript": "^5.7.0"
  }
}
